{
  "schema_version": 1,
  "rule": "latency passes when infer() P50 < p50_ms * (1 + margin). infer() is the full served call: VAE encode, proprio, CUDA Graph replay of prefill and the 10-step denoise loop, denormalize, D2H copy.",
  "devices": [
    {
      "device": "thor",
      "match": {
        "name_contains": "Thor",
        "compute_capability": [
          11,
          0
        ]
      },
      "gated": true,
      "reason": "Jetson AGX Thor, the deployment target",
      "baselines": {
        "nvfp4": {
          "p50_ms": 202.2,
          "margin": 0.05,
          "source": "opportunities.md OPT-030 / THOR_STATUS_SUMMARY.md: the UNTRIMMED reference configuration, eccf14f round, Jetson AGX Thor, MAXN, GPC 1.575 GHz, emc_locked=null, GPU exclusive; gate nvfp4 202.2 ms (pass), end-to-end default row 202.4/202.1/202.0 ms over three repeats (0.4 ms spread). The served default is now the trimmed configuration (profile `default`); its gate run is `--text-trim --manifest .../imagewam_libero_gate_v2.manifest.json` and measured P50 114.6 ms at 0919, so this one-sided bound still holds for both. Supersedes the 231.6 ms baseline, whose session's repeat spread was never measured (issues.md ISSUE-082)."
        }
      }
    },
    {
      "device": "h100",
      "match": {
        "name_contains": "H100",
        "compute_capability": [
          9,
          0
        ]
      },
      "gated": false,
      "reason": "shared H100: another user's training job holds about 40GB and about 100% utilization on every GPU (PROJECT.md, Shared H100 server), so timings are contaminated",
      "baselines": {}
    }
  ]
}
